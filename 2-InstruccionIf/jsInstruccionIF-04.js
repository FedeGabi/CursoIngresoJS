function mostrar()
{
	let edad;
	edad = document.getElementById("txtIdEdad").value;
	edad = parseInt(edad);

	if (edad >= 13 && edad <= 17){

		alert("sos adolescente")
	}
}
	
	//if (edad >= 13 && edad <=17){

		//alert("sos adolescente")
			


	//}
	// operador OR "||" (doble pipe)

	// operador AND "&&" (doble ampersand)

	// operador NOT "!" (signo exclamacion)
	//if ( !(edad < 13 || edad >17)){
             //ver      //fal
			//ver      //fal
		//alert("sos adolescente")
			


	


//FIN DE LA FUNCIÓN

// funcion OR es falso cuando ambos booleanos son falsos, sino siempre es verdadero.
// funcion AND es verdadero cuando ambos boobleanos son verdaderos, sino siempre es falso.